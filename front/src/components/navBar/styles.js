import Styled from "styled-components";

export const Style = Styled.div`

.entrar-admin {
    width: 20px;
    text-decoration: none;
    color: #fff;
    border: 1px solid #fff;
    padding: 3px;
    word-break: break-all;
    border-radius: 3px;
    font-size: 14px;
}

.entra-admin-btn {
    transition: all 0.3s ease-in-out;decoration: none;
    background: transparent;
    border: none;
}

.entra-admin-btn:hover {
    transform: scale(1.08);
    transition: all 0.5s;
}

`;
